<div class="sensor-identity-card">
    <div class="sensor-header">
        <span class="sensor-id">SN-99X-ALPHA</span>
        <span class="status-badge">ONLINE</span>
    </div>
    <div class="sensor-metadata">
        <div class="meta-row"><span>BATCH ID</span><span class="neon-text">BX-2026-XQ</span></div>
        <div class="meta-row"><span>CALIBRATION</span><span class="neon-text">2026-03-01</span></div>
        <div class="meta-row"><span>FIRMWARE</span><span class="neon-text">v4.8.2-AOT</span></div>
        <div class="meta-row"><span>LOCATION</span><span class="neon-text">HUB-7 (TRANSIT)</span></div>
        <div class="meta-row"><span>SIG</span><span class="neon-text">SHA256:0xf9a2...</span></div>
    </div>
</div>

<style>
    .sensor-identity-card { 
        background: rgba(15,23,42,0.6); 
        backdrop-filter: blur(16px); 
        border: 1px solid rgba(56,189,248,0.2); 
        padding: 24px; 
        border-radius: 16px; 
        font-family: 'JetBrains Mono', monospace; 
        transition: all 0.3s ease; 
        box-shadow: inset 0 0 20px rgba(0,0,0,0.5); 
        height: 100%;
        display: flex;
        flex-direction: column;
    }
    .sensor-identity-card:hover { 
        border-color: #38bdf8; 
        box-shadow: inset 0 0 20px rgba(56,189,248,0.1); 
    }
    .sensor-header { 
        display: flex; 
        justify-content: space-between; 
        align-items: center; 
        border-bottom: 1px dashed rgba(255,255,255,0.1); 
        padding-bottom: 16px; 
        margin-bottom: 20px; 
    }
    .sensor-id { 
        font-size: 1.1rem; 
        font-weight: 800; 
        color: #f8fafc; 
        text-shadow: 0 0 10px rgba(56,189,248,0.5); 
    }
    .status-badge { 
        background: rgba(16,185,129,0.1); 
        color: #10b981; 
        padding: 6px 10px; 
        border-radius: 6px; 
        font-size: 0.75rem; 
        font-weight: 700; 
        border: 1px solid rgba(16,185,129,0.3); 
        box-shadow: 0 0 10px rgba(16,185,129,0.2);
    }
    .sensor-metadata { 
        display: flex; 
        flex-direction: column; 
        gap: 16px; 
        flex: 1;
        justify-content: center;
    }
    .meta-row { 
        display: flex; 
        justify-content: space-between; 
        font-size: 0.8rem; 
        align-items: center;
    }
    .meta-row span:first-child { 
        color: #64748b; 
        letter-spacing: 2px; 
        font-weight: 700;
        font-size: 0.65rem;
    }
    .meta-row span:last-child { 
        color: #cbd5e1; 
        font-weight: 600; 
    }
    .neon-text {
        text-shadow: 0 0 8px rgba(255,255,255,0.2);
    }
</style>

@code {
}
