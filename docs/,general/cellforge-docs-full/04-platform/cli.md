# CLI Tool

## Commands

- `cell new <name>` → create cell
- `cell validate` → enforce rules
- `cell benchmark` → measure performance
- `cell doctor` → diagnose issues
- `cell upgrade` → update templates

## Output
- Standard project structure with src, contracts, tests, build, deploy
